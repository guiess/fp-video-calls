// ── App configuration ──────────────────────────────────────────────────────
// Replace these values before running the app.
// For production, use react-native-config or similar to manage per-env values.

/** URL of the fp-video-calls signaling server */
export const SIGNALING_URL = 'https://app-voice-video-server.azurewebsites.net';

/** Web client ID from Firebase console → Auth → Sign-in providers → Google */
export const GOOGLE_WEB_CLIENT_ID =
  '383202942527-lceodkm5iim9sq9f2cqpksdb565mkhvm.apps.googleusercontent.com';
